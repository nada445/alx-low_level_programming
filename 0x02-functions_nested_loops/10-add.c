#include <stdio.h>
#include "main.h"


/**
 * add - adds 2 numbers and prints the result
 * @a : first number
 * @b : second number
 * description: summition
 * Return: Always 0 (Success)
 */
int add(int a, int b)
{
return (a + b);
}
