#include <stdio.h>
#include "main.h"

/**
 * _islower - checks whether the char is lower
 * @c: The character to check
 * description: searching for lower case
 * Return: Always 0 if upper 1 if lowe
 */
int _islower(char c)
{
return (c >= 'a' && c <= 'z');
}
