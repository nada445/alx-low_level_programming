#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
char h=122;
while(h != 96)
{
putchar(h);
h--;
}

putchar('\n');
return (0);
}
